{
    "soundSetName": {
        "type": "string",
        "default": "Click",
        "enum": ["Click"]
    },
    "volume": {
        "type": "number",
        "default": 1,
        "minimum": 0,
        "maximum": 1
    },
    "throttling": {
        "type": "boolean",
        "default": true,
        "description": "wait to complete previous audio playing"
    }
}
